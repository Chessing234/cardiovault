pragma circom 2.0.0;

// Placeholder circuit for CardioVault ZK health proofs (Prompt 03).

template HealthProof() {
    signal input in1;
    signal output out1;

    out1 <== in1;
}

component main = HealthProof();
