"""
Machine learning source for CardioVault (Byte2Beat).

Import concrete modules directly, for example::

    from data_loader import load_and_preprocess
    from model import CardiovascularRiskModel

When running inside the monorepo, add ``packages/ml/src`` to ``PYTHONPATH``.
"""
